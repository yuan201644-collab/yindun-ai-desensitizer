# 「隐盾」演示走查单 — 云端精准增强 · 3–5 分钟简版

> 用途：录屏分镜前的人工核对清单 + 实测数据。**API 链路由脚本实测通过**；前端 UI 交互待录制时逐项勾选确认。

## 0. 演示环境（已就绪）
- 后端 `http://127.0.0.1:8000` — running（v2.0.0）
- 前端 `http://127.0.0.1:5173` — vite dev running
- 测试图：`../testcases/ea4f9...jpg`（身份证正面，5 个敏感区，演示杀手锏最合适）

> ⚠️ **冷启动告警**：OCR 首次加载模型约 **3–5s**，后续热缓存约 **0.5s**。录制前先"热身"一次（随便传张图识别），避免录到长白屏；或分镜里把这个加载期剪掉/加转场。

## 1. 黄金路径 5 步（对照 PPT 第17页）— API 实测数据

| 步骤 | 操作(前端) | API 依据 | 实测 | 讲解点 |
|---|---|---|---|---|
| ① | 识别模式默认「精准增强」→ 上传身份证正图 → 点「🔍 开始识别」 | POST /api/ocr | 0.5s(热)/~5s(冷)，识别出 5 敏感区：姓名high·出生日medium·地址high×2·身份证high，+1对象框(人像) | 端侧信任条"不出设备"(此处是云端精准模式,讲"用完即弃不留存") |
| ② | 魔法框校验 + 点「✏️ 手动画框」拖拽补一个漏检框 | OCR 输出的 rect / 手工 addRegion | 5 敏感区对应 rect | 自动识别不是终点,人工二次修正闭环 |
| ③ | 脱敏方式选「不可逆替换」→ 强度滑条70% → 保护等级「等级3」→ 点「应用脱敏(N处)」 | POST /api/desensitize/image `method=irreversible rounds=3` | 0.4s，regions_processed=5 | 打散重排+种子哈希+3轮,AI还原不了 |
| ④ | 结果面板「👉 去检测脱敏强度」→ 自动带入 → 「🔍 开始强度检测」→ 查看雷达 → 「🖨️ 导出 PDF 报告」 | POST /api/check | 0.1s；`global_risk_score=27·safe`、`adversarial_summary="还原攻击全部失败"`、雷达 `privacy:99 / noise_control:16` | 从"能测"到"能证明"——PDF 留存/合规 |
| ⑤ | (可选/文本页)上下文增强掩码 | POST /api/desensitize/text | 文本页演示"姓名：张三"结构化捕获 | 规则+上下文双通道,端侧可跑 |

## 2. 强度检测可讲的关键数值（写进字幕/旁白）
- 综合评分 **27 分 / safe**；每区 SSIM 0.0128 / PSNR 7.54 / 纹理熵 7.58
- 对抗还原：超分 / 去模糊 / 边缘增强**全部失败**（还原不出原信息）
- 雷达图四维：隐私 99 · 纹理 93 · 内容可用性 24 · 噪声控制 16 —— 隐私拉满、其余可控，正好引出"可调档平衡"

## 3. 潜在坑 / 需你在录制时确认
- [x] 前端 UI 按钮文案已对照源码核实（见 §1 操作列）——「开始识别」/「手动画框」/「应用脱敏(N处)」/「去检测脱敏强度」/「开始强度检测」/「导出 PDF 报告」均已确认
- [x] 识别模式默认即「精准增强」，无需手动切；上传区下方即有 🔒"用完即弃不留存"提示
- [x] 脱敏方式默认「像素化」→ 需手动选「不可逆替换 ⭐」；保护等级默认 1 → 需点「等级3」
- [ ] "导出 PDF"是浏览器原生打印（**无后端端点**）：录制时用 Ctrl+P → 目标另存/打印机选 PDF，需实际打印瞄一眼排版（浅色报告头+雷达图）
- [x] ② 步"手动补漏检框"：点「✏️ 手动画框」进入拖拽（按钮文案变"画框中：拖拽画新框"），松手即加框；不必纠结覆盖循环提示文案
- [ ] OCR 冷启动白屏：先热身 or 分镜剪掉
- [ ] 激进话术取舍：suggestion 文案是通用模板("如需更强可切换不可逆")，我们已用不可逆，别照读

## 4. 录屏规格建议
- 1080p、16:9、全程最多 1 次切场景；字幕走关键点(每步 1 句)
- 推荐顺序：痛点 5s → 三步动手(①②③) → 杀手锏(④报告/PDF) → 收尾口号
- 可用测试图：快递单(识别密度)、营业执照(拆字)、身份证(隐私卖点)随需切换

---
*生成：2026-08-19 · API 链路实测通过（OCR 0.5s + 脱敏 0.4s + 检测 0.1s）*